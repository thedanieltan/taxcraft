# TaxCraft API

The reference API exposes the official TaxCraft country packages through a stateless HTTP interface.

The service does not persist request bodies or calculation results. Operational logging is limited to event type, result status, jurisdiction and tax year.

Run it from the repository root:

```bash
npm start
```
